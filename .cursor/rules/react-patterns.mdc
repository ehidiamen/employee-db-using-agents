
### Create React Pattern Guide

Create `.cursor/rules/react-patterns.mdc`:
```yaml
---
description: React patterns and anti-patterns
globs: ["**/*.tsx", "**/*.jsx"]
alwaysApply: true
---

# React Best Practices (From Experience)

## Component Organization
Structure components by feature:
src/
features/
auth/
components/
hooks/
types/
api/
dashboard/
components/
hooks/
types/
api/
## Custom Hooks
Extract reusable logic:
```typescript
// ❌ Duplicated logic
const [data, setData] = useState(null);
useEffect(() => {
  fetchData().then(setData);
}, []);

// ✅ Custom hook
function useData<T>(url: string) {
  const [data, setData] = useState<T | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);
  
  useEffect(() => {
    fetch(url)
      .then(res => res.json())
      .then(setData)
      .catch(setError)
      .finally(() => setLoading(false));
  }, [url]);
  
  return { data, loading, error };
}
